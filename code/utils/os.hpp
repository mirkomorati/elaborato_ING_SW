//
// Created by Noè Murr on 04/12/2017.
//

#ifndef ELABORATO_ING_SW_OS_HPP
#define ELABORATO_ING_SW_OS_HPP

/*namespace mm {
    bool openWithDefaultBrowser(std::string link) {
        if (not system(nullptr)) return false;
        int i;
#ifdef APPLE
        i = system(fmt::format("open {}", link).c_str());
#elif WINDOWS
        // todo
#else // suppose to be linux
        // todo
#endif
        if (i != 0) return false;
        return true;
    }
}*/

#endif //ELABORATO_ING_SW_OS_HPP
